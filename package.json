{
  "name": "rate-my-tunes",
  "version": "1.0.0",
  "description": "Rate My Tunes - a fullstack music rating app",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && npx tsx --watch src/server.ts",
    "test": "cd backend && npm test",
    "build": "npm install && cd frontend && npm install && npm run build && cd ../backend && npm install && cd ../"
  },
  "keywords": [
    "music",
    "rating",
    "fullstack",
    "vue",
    "express",
    "typescript",
    "postgres",
    "drizzle",
    "tsyringe",
    "vitest"
  ],
  "author": "Nikos Sidiropoulos, Dennis Hritcu",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/vue": "^8.1.0",
    "@types/testing-library__vue": "^5.0.0",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
